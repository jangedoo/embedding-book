# Retrieval as Geometry

Dense retrieval turns search into nearest-neighbor geometry.

A query encoder maps a query to a vector. A document encoder maps documents to vectors. Retrieval finds documents close to the query.

In retrieval, queries and documents often have different distributions. This is why query/document prompts and asymmetric training matter.
