# Objectives Shape Embedding Spaces

The same objects can form different embedding spaces under different objectives.

Classification may create class-separated clusters. Contrastive learning may create neighborhoods that preserve pairwise relevance. Language modeling may create token representations useful for next-token prediction, not necessarily sentence-level semantic retrieval.

Never ask only: "Are these embeddings good?" Ask: "Good for what objective, metric, and downstream decision?"
