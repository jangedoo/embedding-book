# Embeddings as Matrix Factorization

Many embedding systems can be viewed as factorizing a relationship matrix.

Examples:

- word-context matrix
- user-item interaction matrix
- query-document relevance matrix

Low-rank factorization assumes the observed relationships can be explained by fewer latent factors.
