# Token Embeddings in Language Models

Language models use token embeddings to convert token IDs into vectors.

Important components include the input embedding table, positional information, transformer layers, output head, and possible weight tying.

Post-training vocab pruning can save memory, but it changes what token IDs mean.
